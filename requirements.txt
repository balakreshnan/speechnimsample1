# The NVIDIA blueprint currently declares Python <3.14, but Pipecat 1.3.0 and
# these native runtime extras have been verified in this project's Python 3.14.7 venv.
pipecat-ai[nvidia,openai,runner,silero,webrtc,websocket]==1.3.0
python-dotenv>=1.2.2
loguru>=0.7.3
PyYAML>=6.0.3
pypdf>=6.0.0
